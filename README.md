# Terminal Agent

AI-powered CLI command generator that converts natural language into terminal commands.

## Usage

```
bash
./terminal_agent.sh "find all PDF files modified in the last 24 hours"
```
